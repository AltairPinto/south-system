import React from 'react';
import { BookSearch, SearchBar } from '../../components';

const Home: React.FC = () => {
  return (
    <>
      <SearchBar />
      <BookSearch />
    </>
  );
};

export default Home;
